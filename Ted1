# Programa para coletar altura e gênero de 15 pessoas, mostrar maior e menor altura, média dos homens e número de mulheres

maior_altura = 0
menor_altura = 0
soma_altura_masc = 0
qtd_masc = 0
qtd_fem = 0

for i in range(1, 16):
    print(f"\nPessoa {i}:")
    altura = float(input("Digite a altura: "))
    genero = input("Digite o gênero (M/F): ").upper()

    # Maior e menor altura
    if i == 1:
        maior_altura = altura
        menor_altura = altura
    else:
        if altura > maior_altura:
            maior_altura = altura
        if altura < menor_altura:
            menor_altura = altura

    # Contador e soma
    if genero == 'M':
        soma_altura_masc += altura
        qtd_masc += 1
    elif genero == 'F':
        qtd_fem += 1

if qtd_masc > 0:
    media_altura_masc = soma_altura_masc / qtd_masc
else:
    media_altura_masc = 0

print("\n--- RESULTADOS ---")
print(f"Maior altura do grupo: {maior_altura:.2f} m")
print(f"Menor altura do grupo: {menor_altura:.2f} m")
print(f"Média de altura dos homens: {media_altura_masc:.2f} m")
print(f"Número de mulheres: {qtd_fem}")
